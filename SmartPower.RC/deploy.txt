echo 'export DOTNET_GCHeapHardLimit=536870912' >> ~/.bashrc
source ~/.bashrc

dotnet publish -r linux-arm64 --self-contained true -c Release /p:PublishSingleFile=true

mkdir -p /home/smartpower/smartpower.rc

# delete everything in destination folder
rm -rf ./*

scp -r /home/smartpower/SmartPower.RC/bin/Release/net10.0/linux-arm64/publish/. root@192.168.4.1:/home/smartpower/smartpower.rc/

scp -r . root@192.168.4.1:/home/smartpower/smartpower.rc/

xbps-install -S icu
# export DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=1

chmod +x /home/smartpower/smartpower.rc/SmartPower.RC

mkdir -p /etc/sv/smartpower


